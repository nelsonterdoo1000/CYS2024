import api from '../../api/api'

const EntryManager = async (token, user, setUser) => {

    // if (Number(user?.id) * 1) {
    //     try {
    //         const response = await api.get(`/reg/view-attendee/${user?.id}`)
    //         console.log(response.data,user?.id)
    //         setUser(prev => ({ ...prev, access_token: token?.access, user_id: response.data.id, resUser: response.data }))
    //     } catch (error) {
    //         console.log(error)
    //     }

    // }
}

export default EntryManager